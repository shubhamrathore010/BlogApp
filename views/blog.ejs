<!DOCTYPE html>
<html lang="en">
<head>

    <style>
      
        .container {
            background-color: white;
            box-sizing: border-box;
            overflow: hidden; 
            box-sizing: border-box;   
            max-width: 700px;     
        }
        p {
            overflow: hidden; 
            box-sizing: border-box;   
            max-width: 700px;  
            justify-content: center;
            align-items: center;
             
            white-space: pre-wrap;  
            overflow: auto;  
        }
        .proImg {
            background-color: rgb(234, 241, 105);
            box-sizing: border-box;   
            max-width: 50px;
            border-radius: 50%;
            border: 1px solid black;
        }
      </style>

    <%- include('./partials/head') -%>
    <title>Blog</title>
</head>
<body style="background-color: rgb(240, 239, 237)">
    
    <%- include('./partials/nav') -%>
<div class="content">
    <div class="container">
        <div class="img ">
            <img src="<%= blog.coverImageURL %>" height="200px" max-width="400px" />
        </div> 

      <div class="contnainer mt-3">
        <h1><%= blog.title %></h1>
    </div> 
       
    <div class="container mt-4">
    <div class="proImg"><img src="<%= blog.createdBy.profileImageURL %>"  width="50px"></div>
         <%= blog.createdBy.fullName %>
    </div>
    <pre><p class="mt-3"><code><%= blog.body %></code</p>></pre>
</div>

    <div class="container mt-3">
     <h1>Comments (<%= comments.length %>)</h1>
     <% if (locals.user) { %>
        <form action="/blog/comment/<%= blog._id %>" method="post">
            <div class="mb-3">
                <input type="text" name="content" class="form-control" placeholder="Enter your Comment">
              <button class="btn btn-sm btn-primary mt-2" type="submit">Add</button>
               </div>
           </form>
           <% } %>
           <div class="cmt mt-3">
            <% comments.forEach(comment => { %>
             <di v>
            <div class="proImg"><img src="<%= comment.createdBy.profileImageURL %>"  width="50px"></div> 
                <%= comment.createdBy.fullName %>
                <pre><%= comment.content %></pre>
             </div>
            <% }) %>
           </div>  
    </div>
    <%- include('./partials/scripts') -%>
</body>
</html>